node_modules/
.astro/

# Local-only — regenerated by scripts. Not needed on the deploy server.
reference/
src/captured/
.env
.env.local
.DS_Store
*.log
.cloudflare/
.vercel/

# Deploy upload artifact (built from dist/, regenerated on demand)
web-ziny-upload.zip

# Empty leftover dir from create-astro, locked by OneDrive; delete later
green-galaxy/
